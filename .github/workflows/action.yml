name: Publish To gh-pages

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: beta
      - run: flutter pub get
      - run: pwd
      - run: git status
      - run: echo "www.mkg.company" > CNAME
      - run: ls
      - uses: bluefireteam/flutter-gh-pages@v7
        with:
          basehref: /
